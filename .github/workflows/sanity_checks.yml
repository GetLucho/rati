name: Sanity Checks

on: [push, pull_request]

jobs:
  build:
    runs-on: ubuntu-latest

    strategy:
      fail-fast: false
      matrix:
        # Every combination that ships: both backends, neither, and each alone.
        features:
          - ""
          - "--no-default-features"
          - "--no-default-features --features s3"
          - "--no-default-features --features azure"

    name: ${{ matrix.features || 'default features' }}

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Rust
        uses: actions-rs/toolchain@v1
        with:
          toolchain: stable
          components: rustfmt, clippy

      - name: Check formatting
        run: cargo fmt --all -- --check

      - name: Run Clippy
        run: cargo clippy --all-targets ${{ matrix.features }} -- -Dwarnings

      - name: Run tests
        run: cargo test ${{ matrix.features }}
